<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.travel.agent.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.travel.agent.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="email" column="email"/>
        <result property="password" column="password"/>
        <result property="avatar" column="avatar"/>
        <result property="phone" column="phone"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="findByUsername" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, email, password, avatar, phone, status, created_at, updated_at
        FROM users
        WHERE username = #{username}
    </select>

    <select id="findByEmail" parameterType="string" resultMap="UserResultMap">
        SELECT id, username, email, password, avatar, phone, status, created_at, updated_at
        FROM users
        WHERE email = #{email}
    </select>

    <select id="findById" parameterType="long" resultMap="UserResultMap">
        SELECT id, username, email, password, avatar, phone, status, created_at, updated_at
        FROM users
        WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.travel.agent.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (username, email, password, avatar, phone, status, created_at, updated_at)
        VALUES (#{username}, #{email}, #{password}, #{avatar}, #{phone}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.travel.agent.entity.User">
        UPDATE users
        SET username = #{username},
            email = #{email},
            password = #{password},
            avatar = #{avatar},
            phone = #{phone},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>
